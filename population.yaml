
edges_timestamps_graph(raw_data): 
4 x n_of_edges array
edge = source target start end 

RETURNS edge_list

get_path_features(raw_data): 
duration x features -for each individual
duration x features x n_of_individuals  -for population

RETURNS individual_features_array, population_features_array

distance_traveled(raw_data/results):
duration x n_of_individuals
total = row - sum for population
RETURNS 

retention_heatmap(raw_data): 
2d array
RETURNS retention_array

global_graph_measures(G): 
Column/row n_of_measures
RETURNS global_graph_measures

individual_graph_measures(G):
2d array n_of_measures x n_of_individuals
RETURNS individual_graph_measures

measures_correlation(result_table):
len_of_result_table x len_of_result_table
RETURNS measures_correlation_matrix



raw_data_path
datum
vrijeme
pop
tretman
broj_jedniki
izolirani/grupirani
djevci/pareni
svjetlo
trajanje_videa
napomena 

adjacency_matrix:

dynamic_graph:

graph_measures:
    global:

    middle:

    local:


